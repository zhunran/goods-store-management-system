server:
  port: 8086

spring:
  application:
    name: goods-store-trade-api
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        group: DEFAULT_GROUP
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        group: DEFAULT_GROUP
        file-extension: yaml
        namespace: public
  config:
    import:
      - optional:nacos:goods-store-common.yaml
      - optional:nacos:goods-store-shared-service-config.yaml

snowflake:
  worker-id: 5
  datacenter-id: 1
