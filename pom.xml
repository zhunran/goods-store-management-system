<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.fengluan</groupId>
    <artifactId>goods-store-management-system-parent</artifactId>
    <version>1.0.0</version>
    <name>goods-store-management-system-parent</name>
    <description>总父项目</description>
    <packaging>pom</packaging>

    <modules>
        <module>goods-store-common</module>
        <module>goods-store-gateway</module>
        <module>goods-store-service</module>
        <module>goods-store-spi</module>
        <module>goods-store-web</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <!-- Spring Boot 版本由 spring-boot-starter-parent 统一管理，勿在此重复声明 -->
        <spring-cloud-dependencies.version>2025.1.3</spring-cloud-dependencies.version>
        <spring-cloud-alibaba-dependencies.version>2025.1.0.0</spring-cloud-alibaba-dependencies.version>
        <mybatis-plus.version>3.5.17</mybatis-plus.version>
        <redisson.version>3.50.0</redisson.version>
        <jjwt.version>0.12.6</jjwt.version>
        <hutool.version>5.8.38</hutool.version>
        <druid.version>1.2.25</druid.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- ==================== Spring 生态 BOM ==================== -->
            <!-- spring-boot-dependencies 已由 spring-boot-starter-parent 传递，无需重复 import -->

            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud-dependencies.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba-dependencies.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ==================== MyBatis-Plus（Day 2 数据访问层） ==================== -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-extension</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- 分页插件 3.5.9+ 需单独引入 jsqlparser -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-annotation</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- ==================== 中间件客户端 ==================== -->
            <!-- Redisson 分布式锁（Day 10 下单防重、Day 13 秒杀） -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>
            <!-- Druid 连接池（Day 2）；
                 若在 Boot 4 下自动装配异常，降级方案：改引 druid 核心包 + 手动注册 DataSource -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- ==================== 工具库 ==================== -->
            <!-- JWT 双令牌（Day 3 认证服务） -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <!-- Hutool（雪花算法 IdUtil、加密、日期等） -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

</project>
